# Force Coolify to use Dockerfile instead of Nixpacks
[providers]
dockerfile = true

[phases.setup]
nixPkgs = []

[phases.install]
cmds = []

[phases.build]
cmds = []

[start]
cmd = "apache2-foreground"
